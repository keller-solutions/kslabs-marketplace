{
  "description": "KS Core guardrails: git never-events denied deterministically; risky-but-sometimes-sanctioned actions ask first. Only universally-safe rules live here — pipeline-specific gates belong in skill-scoped hooks.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/git-guardrails.sh"
          }
        ]
      }
    ]
  }
}
